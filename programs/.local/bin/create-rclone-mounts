#!/bin/bash

rclone mount --daemon --dir-cache-time 30m --vfs-cache-mode full drive-volatile: "$HOME/Drive"
rclone mount --daemon --dir-cache-time 30m --vfs-cache-mode full drive-persistent: "$HOME/Drive_Persistent"
